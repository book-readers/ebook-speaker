To compile eBook-speaker, be sure the following extra packages are installed:

   libncursesw5-dev
   libidn11-dev
   libsox-dev
   libzip-dev

Give the following command:

   $ make

run the script install.sh, as user root, to install eBook-speaker onto
your system:

   # ./install.sh
