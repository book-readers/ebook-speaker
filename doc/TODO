- How to read <text between < and >? >
- Composite_Document_File.xls
- search text in loaded book.
- mathml. Convert with natbraille
- Replace the external command iconv by the function iconv ()
- translate current documant with trans
- if started from a dir, return to that dir after finishing a book
- getwc
